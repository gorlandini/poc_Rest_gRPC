server:
  port: 8081

grpc:
  server:
    port: 9090

spring:
  application:
    name: grpc-produtos-api

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus

logging:
  level:
    root: INFO
