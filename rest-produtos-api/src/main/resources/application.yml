server:
  port: 8080

spring:
  application:
    name: rest-produtos-api

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus

logging:
  level:
    root: INFO
